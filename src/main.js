import { VimForKidsGame } from './VimForKidsGame.js';

// Initialize game when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    window.vimForKidsGame = new VimForKidsGame();
});
